// npx prettier .\src\scripts\prettier-bad.js --write

function sayHello(name) {
  console.log('Hello, ' + name + '!');
}
const nums = [1, 2, 3, 4, 5];
nums.forEach((n) => {
  console.log(n * 2);
});
